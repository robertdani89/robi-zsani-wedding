import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'robi.zsani.wedding',
  appName: 'robi-zsani-wedding',
  webDir: 'dist/robi-zsani-wedding/browser',
};

export default config;
